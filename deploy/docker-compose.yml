services:
  inference:
    build: ..
    image: cats-dogs-inference:latest
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=/app/models/model.keras
      - LABEL_MAP_PATH=/app/models/label_map.json
    volumes:
      - ../models:/app/models:ro
